/** Default age (ms) after which an active blocker is flagged as potentially stuck. */
export const DEFAULT_STUCK_THRESHOLD_MS = 10_000;

/** Interval (ms) at which the active-blockers view refreshes blocker ages. */
export const STUCK_TICK_INTERVAL_MS = 1_000;
